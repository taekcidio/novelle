{
  "name": "novelle-frontend",
  "version": "1.0.0",
  "description": "Novelle — Plataforma de historias interactivas con IA",
  "scripts": {
    "dev": "sass --watch scss/main.scss:css/main.css --style=expanded",
    "build": "sass scss/main.scss:css/main.css --style=compressed --no-source-map",
    "build:dev": "sass scss/main.scss:css/main.css --style=expanded"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
